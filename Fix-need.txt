#Add transaction ui #
I need you to fix and improve the following issues in my Next.js FinFlow project across the **Add Transaction** page which has 4 tabs: **Manual, NLP, Voice, and Scan**.

---

### **Issue 1: AI Confidence Display & UI Fixes on Scan/AI Result Card**

**Problem:**
- The Gemini API response is showing AI Confidence as `0.95%` instead of `95%`. The value returned from Gemini is likely `0.95` (a decimal between 0 and 1), but it's being displayed with a `%` sign directly — making it show `0.95%`. Fix the parsing so that if the value is between 0 and 1, multiply by 100 before displaying (e.g., `0.95` → `95%`).
- The **"Confirm Save"** and **"Discard"** buttons are on separate lines. Place **Confirm** and **Discard** on the **same row/line** as inline actions.
- The **"Edit"** button is currently a full-width outlined button. Make it a **small icon button** (pencil icon only) positioned at the top-right corner of the result card instead.
- The **Manual tab** is opening by default with **red-themed styling** (red ₹ symbol, red amount text, red Expense button). Change the default appearance so the Manual tab opens with **normal/neutral white or the app's standard teal/green theme colors** — not red. Red should only appear contextually if needed (e.g., for expense type indication after selection), but the initial/default state should look clean and neutral (white background, teal/green accents consistent with the rest of the app).

---

### **Issue 2: Bulk/Multiple Transaction Support in Voice & NLP**

**Problem:**
- Currently, when a user types something like *"Spent on food 2000 and transport 290"* in **NLP**, or speaks the same in **Voice**, the system either fails with a server error or only parses one transaction.

**Required Fix:**
- Both **NLP** and **Voice** input modes must support **bulk/multiple transactions** in a single input.
- When the AI (Gemini) parses the input, it should detect multiple transactions and return an **array of transaction objects**.
- The UI should then display **a list/stack of parsed transaction cards** — each with its own amount, category, type (income/expense), and individual Confirm/Edit/Discard actions.
- Example input: *"Spent 2000 on food and 290 on transport and received 5000 salary"*
  - Should produce **3 separate transaction cards**: Food ₹2000 (Expense), Transport ₹290 (Expense), Salary ₹5000 (Income).
- Update the Gemini prompt/API call to explicitly instruct it to return multiple transactions as a JSON array when multiple items are detected in the user's input.
- Handle the frontend rendering to loop through and display each transaction result independently.

---

### **Issue 3: Voice UI — Replace Red Microphone with Three.js Interactive Particle Effect**

**Problem:**
- The current Voice tab UI uses a **large red microphone circle** with red waveform bars, red text ("Listening... Tap to stop"), and a red progress bar. This looks harsh and inconsistent with the app's teal/green theme.

**Required Fix:**
- **Remove all red color** from the Voice UI entirely. No red microphone button, no red waveform, no red text, no red progress bar.
- **Replace the current voice recording visual** with an interactive **Three.js particle/sphere animation** that reacts to the mouse cursor (on desktop) and touch (on mobile).
- The Three.js effect should:
  - Display an **animated particle sphere or waveform mesh** in the center of the Voice tab area.
  - Use the app's **teal/green and white color palette** (e.g., teal particles, white glow, subtle green accents). **No red at all.**
  - **React to mouse movement** — particles should subtly attract/repel or ripple based on cursor proximity (use raycasting or simple distance-based displacement).
  - When **actively recording/listening**, the particles should animate more vigorously (pulse, expand, wave) to indicate the mic is live.
  - When **idle/stopped**, the particles should be calm and gently floating.
  - Include a tap/click interaction on the sphere itself to **start/stop recording** (replacing the old red mic button).
- Keep the text labels like "Listening... Tap to stop" and "Supports Hindi, Bengali, Tamil, Telugu & more" but style them in **white or teal** — not red.
- Keep the timer display (e.g., "1s / 15s") and Cancel button but restyle them in the app's neutral/teal theme.
- Use `@react-three/fiber` and `@react-three/drei` for the Three.js integration in Next.js. Make sure to handle SSR properly (dynamic import with `ssr: false`).
- Ensure it is **performant on mobile devices** — use a reasonable particle count and simplify the geometry if needed.

---

### **Summary of All Changes Needed:**

| # | Area | Fix |
|---|------|-----|
| 1a | Scan Result Card | AI Confidence: multiply decimal by 100 before displaying with % |
| 1b | Scan Result Card | Put Confirm & Discard on the same line |
| 1c | Scan Result Card | Make Edit a small pencil icon button (top-right of card) |
| 1d | Manual Tab | Default state should be white/neutral themed, not red |
| 2 | NLP + Voice | Support bulk/multiple transactions in a single input; display as multiple result cards |
| 3 | Voice Tab UI | Replace red mic animation with Three.js interactive particle effect using teal/white colors, no red anywhere in Voice UI |

---




Please implement all fixes with clean, production-ready code. Handle edge cases, mobile responsiveness, and SSR compatibility for Three.js.
